name: Flatpak — Build & Publish to GitHub Pages

on:
  # Auto-trigger when a new release is published
  release:
    types: [published]
  # Allow manual trigger from the Actions tab
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to build (e.g. v1.1.0)'
        required: true
        default: 'v1.1.0'

permissions:
  contents: write   # to upload release assets
  pages: write      # to deploy to GitHub Pages
  id-token: write   # for Pages OIDC token

jobs:
  # ── Job 1: Download the .deb on ubuntu-latest (gh CLI available here) ────
  prepare:
    name: Extract binary from release
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag.outputs.value }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set release tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            echo "value=${{ github.event.release.tag_name }}" >> "$GITHUB_OUTPUT"
          else
            echo "value=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Download release .deb and extract binary
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.tag.outputs.value }}"
          echo "Preparing binary for release: $TAG"

          mkdir -p /tmp/release
          gh release download "$TAG" \
            --repo "${{ github.repository }}" \
            --pattern "*.deb" \
            --dir /tmp/release

          DEB=$(ls /tmp/release/*.deb | head -1)
          echo "Found deb: $DEB"

          dpkg-deb -x "$DEB" /tmp/extracted
          cp /tmp/extracted/usr/bin/recopied flatpak/recopied
          chmod +x flatpak/recopied
          echo "Binary ready: $(file flatpak/recopied)"

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: recopied-binary
          path: flatpak/recopied
          retention-days: 1

  # ── Job 2: Build the Flatpak inside the official GNOME SDK container ─────
  build-flatpak:
    name: Build Flatpak bundle
    runs-on: ubuntu-latest
    needs: prepare
    container:
      image: bilelmoussaoui/flatpak-github-actions:gnome-47
      options: --privileged
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download recopied binary
        uses: actions/download-artifact@v4
        with:
          name: recopied-binary
          path: flatpak/

      - name: Make binary executable
        run: chmod +x flatpak/recopied

      - name: Build Flatpak
        uses: bilelmoussaoui/flatpak-github-actions/flatpak-builder@v6
        with:
          bundle: recopied.flatpak
          manifest-path: flatpak/com.recopied.app.yml
          cache: true
          cache-key: flatpak-v1-gnome47-${{ hashFiles('flatpak/com.recopied.app.yml') }}

      - name: Upload bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: flatpak-bundle
          path: recopied.flatpak
          retention-days: 1

  # ── Job 3: Upload bundle to release + deploy ostree repo to Pages ─────────
  publish:
    name: Upload to release & publish Pages repo
    runs-on: ubuntu-latest
    needs: [prepare, build-flatpak]
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download Flatpak bundle
        uses: actions/download-artifact@v4
        with:
          name: flatpak-bundle

      - name: Install flatpak tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y flatpak ostree

      - name: Upload Flatpak bundle to GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ needs.prepare.outputs.tag }}"
          gh release upload "$TAG" recopied.flatpak \
            --repo "${{ github.repository }}" \
            --clobber

      - name: Export bundle into static ostree repo
        run: |
          mkdir -p repo
          ostree init --mode=archive-z2 --repo=repo/
          flatpak build-import-bundle repo/ recopied.flatpak
          flatpak build-update-repo \
            --generate-static-deltas \
            --default-branch=stable \
            repo/

      - name: Add .flatpakrepo file and index page
        run: |
          cat > repo/recopied.flatpakrepo << 'REPO'
          [Flatpak Repo]
          Title=Recopied
          Url=https://mrbeandev.github.io/Recopied/
          Homepage=https://github.com/mrbeandev/Recopied
          Comment=Clipboard manager for Linux with Win+V shortcut
          Description=Self-hosted Flatpak repository for Recopied
          Icon=https://raw.githubusercontent.com/mrbeandev/Recopied/main/src-tauri/icons/128x128.png
          GPGVerify=false
          DefaultBranch=stable
          REPO

          TAG="${{ needs.prepare.outputs.tag }}"
          cat > repo/index.html << 'HTMLEOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
          <meta charset="UTF-8">
          <meta name="viewport" content="width=device-width, initial-scale=1.0">
          <title>Recopied — Clipboard Manager for Linux</title>
          <meta name="description" content="A lightweight clipboard history manager for Linux inspired by Windows Win+V. Copy once, paste forever.">
          <style>
          *{margin:0;padding:0;box-sizing:border-box}
          :root{--bg:#0d1117;--surface:#161b22;--surface2:#1c2333;--border:#30363d;--text:#e6edf3;--text2:#8b949e;--accent:#58a6ff;--accent2:#388bfd;--gradient:linear-gradient(135deg,#58a6ff 0%,#a371f7 100%)}
          body{font-family:-apple-system,'Segoe UI',Helvetica,Arial,sans-serif;background:var(--bg);color:var(--text);line-height:1.6;overflow-x:hidden}
          a{color:var(--accent);text-decoration:none}
          a:hover{text-decoration:underline}

          .hero{text-align:center;padding:4rem 1.5rem 3rem;position:relative;overflow:hidden}
          .hero::before{content:'';position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:radial-gradient(circle at 50% 50%,rgba(88,166,255,0.08) 0%,transparent 50%);pointer-events:none}
          .hero-icon{width:80px;height:80px;border-radius:20px;margin:0 auto 1.5rem;background:var(--gradient);display:flex;align-items:center;justify-content:center;box-shadow:0 8px 32px rgba(88,166,255,0.25)}
          .hero-icon svg{width:40px;height:40px;color:#fff}
          .hero h1{font-size:2.8rem;font-weight:800;letter-spacing:-0.03em;margin-bottom:0.5rem;background:var(--gradient);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
          .hero .tagline{font-size:1.2rem;color:var(--text2);max-width:500px;margin:0 auto 2rem}
          .hero .version-badge{display:inline-block;background:var(--surface2);border:1px solid var(--border);border-radius:999px;padding:0.3rem 1rem;font-size:0.8rem;color:var(--text2);margin-bottom:2rem}
          .hero .version-badge span{color:var(--accent);font-weight:600}

          .screenshot{max-width:380px;margin:0 auto 1rem;border-radius:12px;border:1px solid var(--border);box-shadow:0 16px 64px rgba(0,0,0,0.4);overflow:hidden}
          .screenshot img{width:100%;display:block}

          .downloads{max-width:720px;margin:0 auto;padding:2rem 1.5rem}
          .downloads h2{font-size:1.4rem;font-weight:700;margin-bottom:1.5rem;text-align:center}
          .download-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1rem;margin-bottom:2rem}
          .download-card{background:var(--surface);border:1px solid var(--border);border-radius:12px;padding:1.5rem;text-align:center;transition:border-color 0.2s,transform 0.2s}
          .download-card:hover{border-color:var(--accent);transform:translateY(-2px)}
          .download-card .format{font-size:0.75rem;text-transform:uppercase;letter-spacing:0.08em;color:var(--text2);margin-bottom:0.5rem}
          .download-card .name{font-size:1rem;font-weight:600;margin-bottom:0.3rem}
          .download-card .desc{font-size:0.8rem;color:var(--text2);margin-bottom:1rem}
          .download-card .btn{display:inline-block;background:var(--gradient);color:#fff;padding:0.5rem 1.5rem;border-radius:8px;font-size:0.85rem;font-weight:600;transition:opacity 0.2s}
          .download-card .btn:hover{opacity:0.9;text-decoration:none}

          .flatpak-section{max-width:720px;margin:0 auto;padding:0 1.5rem 2rem}
          .flatpak-box{background:var(--surface);border:1px solid var(--border);border-radius:12px;padding:1.5rem 2rem}
          .flatpak-box h3{font-size:1.1rem;font-weight:600;margin-bottom:0.8rem;display:flex;align-items:center;gap:0.5rem}
          .flatpak-box pre{background:var(--bg);border:1px solid var(--border);border-radius:8px;padding:1rem;overflow-x:auto;font-size:0.85rem;line-height:1.8;margin-bottom:0.5rem}
          .flatpak-box code{font-family:'SF Mono',Consolas,'Liberation Mono',Menlo,monospace}
          .flatpak-box .hint{font-size:0.78rem;color:var(--text2);margin-top:0.5rem}
          .flatpak-box .hint a{color:var(--accent)}

          .features{max-width:720px;margin:0 auto;padding:1rem 1.5rem 2rem}
          .features h2{font-size:1.4rem;font-weight:700;margin-bottom:1.5rem;text-align:center}
          .feature-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1rem}
          .feature{background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:1.2rem}
          .feature .icon{font-size:1.3rem;margin-bottom:0.5rem}
          .feature .title{font-weight:600;font-size:0.9rem;margin-bottom:0.3rem}
          .feature .desc{font-size:0.8rem;color:var(--text2)}

          footer{text-align:center;padding:2rem 1.5rem 3rem;border-top:1px solid var(--border);margin-top:2rem;color:var(--text2);font-size:0.85rem}
          footer .links{display:flex;justify-content:center;gap:1.5rem;margin-bottom:0.8rem}
          footer .links a{color:var(--text2);transition:color 0.2s}
          footer .links a:hover{color:var(--accent);text-decoration:none}
          .star-btn{display:inline-flex;align-items:center;gap:0.4rem;background:var(--surface2);border:1px solid var(--border);border-radius:8px;padding:0.4rem 1rem;font-size:0.85rem;color:var(--text);transition:border-color 0.2s}
          .star-btn:hover{border-color:var(--accent);text-decoration:none}

          @media(max-width:600px){.hero h1{font-size:2rem}.hero .tagline{font-size:1rem}.download-grid{grid-template-columns:1fr}}
          </style>
          </head>
          <body>

          <section class="hero">
            <div class="hero-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="8" height="4" x="8" y="2" rx="1" ry="1"/><path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"/></svg>
            </div>
            <h1>Recopied</h1>
            <p class="tagline">A lightweight clipboard history manager for Linux &mdash; inspired by Windows <kbd style="background:var(--surface2);border:1px solid var(--border);border-radius:4px;padding:0.1em 0.4em;font-size:0.9em">Win+V</kbd></p>
            <div class="version-badge">Latest: <span>v1.2.0</span></div>
            <div class="screenshot">
              <img src="https://raw.githubusercontent.com/mrbeandev/Recopied/main/public/screenshot-main.png" alt="Recopied clipboard history popup" loading="lazy">
            </div>
          </section>

          <section class="downloads">
            <h2>Download</h2>
            <div class="download-grid">
              <div class="download-card">
                <div class="format">.deb package</div>
                <div class="name">Debian / Ubuntu / Mint</div>
                <div class="desc">Install with dpkg or double-click</div>
                <a class="btn" href="https://github.com/mrbeandev/Recopied/releases/latest/download/Recopied_1.2.0_amd64.deb">Download .deb</a>
              </div>
              <div class="download-card">
                <div class="format">.AppImage</div>
                <div class="name">Universal Linux</div>
                <div class="desc">Portable &mdash; just run it</div>
                <a class="btn" href="https://github.com/mrbeandev/Recopied/releases/latest/download/Recopied_1.2.0_amd64.AppImage">Download AppImage</a>
              </div>
              <div class="download-card">
                <div class="format">Flatpak</div>
                <div class="name">All Distros</div>
                <div class="desc">Sandboxed &amp; auto-updating</div>
                <a class="btn" href="recopied.flatpakrepo" download>Add Repository</a>
              </div>
            </div>
          </section>

          <section class="flatpak-section">
            <div class="flatpak-box">
              <h3><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="4 17 10 11 4 5"/><line x1="12" x2="20" y1="19" y2="19"/></svg> Install via terminal</h3>
              <pre><code>flatpak remote-add --if-not-exists --from recopied \
    https://mrbeandev.github.io/Recopied/recopied.flatpakrepo
flatpak install recopied com.recopied.app</code></pre>
              <p class="hint">Or download the <a href="https://github.com/mrbeandev/Recopied/releases/latest">latest .deb / .AppImage from GitHub Releases</a></p>
            </div>
          </section>

          <section class="features">
            <h2>Features</h2>
            <div class="feature-grid">
              <div class="feature">
                <div class="icon">&#9000;</div>
                <div class="title">Global Shortcut</div>
                <div class="desc">Press Ctrl+Shift+V to open clipboard history from anywhere</div>
              </div>
              <div class="feature">
                <div class="icon">&#128269;</div>
                <div class="title">Instant Search</div>
                <div class="desc">Filter through your clipboard history in real time</div>
              </div>
              <div class="feature">
                <div class="icon">&#128204;</div>
                <div class="title">Pin Items</div>
                <div class="desc">Keep important snippets accessible at the top</div>
              </div>
              <div class="feature">
                <div class="icon">&#128247;</div>
                <div class="title">Image Support</div>
                <div class="desc">Captures and previews clipboard images</div>
              </div>
              <div class="feature">
                <div class="icon">&#128276;</div>
                <div class="title">Copy Notifications</div>
                <div class="desc">System notification confirms every successful copy</div>
              </div>
              <div class="feature">
                <div class="icon">&#9881;</div>
                <div class="title">Lightweight</div>
                <div class="desc">Rust + Tauri backend &mdash; minimal resource usage</div>
              </div>
            </div>
          </section>

          <footer>
            <div class="links">
              <a href="https://github.com/mrbeandev/Recopied">GitHub</a>
              <a href="https://github.com/mrbeandev/Recopied/releases">All Releases</a>
              <a href="https://github.com/mrbeandev/Recopied/issues">Report a Bug</a>
            </div>
            <a class="star-btn" href="https://github.com/mrbeandev/Recopied/stargazers">&#11088; Star on GitHub</a>
            <p style="margin-top:1rem">Built with <a href="https://v2.tauri.app">Tauri</a> &bull; MIT License</p>
          </footer>

          </body>
          </html>
          HTMLEOF

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: repo/

      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4
